"use strict";
(() => {
    var _a;
    const batman = 'Batman';
    const linternaVerde = 'Linterna verde';
    const volcanNegro = `Héroe: Volcan Negro`;
    const abc = '123';
    console.log(`I'm ${batman}, ${abc}`);
    console.log(volcanNegro.toUpperCase());
    console.log((_a = batman[11]) === null || _a === void 0 ? void 0 : _a.toUpperCase());
})();
